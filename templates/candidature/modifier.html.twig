{% extends 'base.html.twig' %}

{% block title %}Modifier Candidature{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card border-0 shadow-sm">
                <div class="card-header bg-white border-bottom-0 pt-4 pb-0">
                    <h4 class="mb-0 text-center">Modifier la Candidature</h4>
                </div>
                <div class="card-body p-4">
                    {{ form_start(form) }}
                        {{ form_row(form.id_candidature) }}
                        {{ form_row(form.chercheur_id) }}
                        {{ form_row(form.date_pro) }}
                        {{ form_row(form.statut) }}
                        {{ form_row(form.annonce_id) }}

                        <div class="d-grid gap-2 mt-4">
                            <button type="submit" class="btn btn-primary btn-lg">Mettre à jour</button>
                            <a href="{{ path('app_candidature') }}" class="btn btn-link text-decoration-none">Annuler</a>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}