<!DOCTYPE html>
<html lang="{{ app.request.locale }}" {% if app.request.locale == 'ar' %} dir="rtl" {% endif %}>
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
		<meta name="theme-color" content="#667eea">
		<meta name="apple-mobile-web-app-capable" content="yes">
		<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
		<meta name="apple-mobile-web-app-title" content="Bricolage">
		<link rel="manifest" href="{{ asset('manifest.json') }}">
		<link rel="apple-touch-icon" href="{{ asset('img/icon-192.png') }}">
		<title>
			{% block title %}Bricolage
			{% endblock %}
		</title>
		<script>
			// Register Service Worker
if ('serviceWorker' in navigator) {
window.addEventListener('load', () => {
navigator.serviceWorker.register('/sw.js').then(registration => {
console.log('SW registered: ', registration);
}).catch(registrationError => {
console.log('SW registration failed: ', registrationError);
});
});
}

// Immediate theme initialization to prevent flash
(function () {
const savedTheme = localStorage.getItem('theme');
const userTheme = "{{ app.user ? app.user.theme : '' }}";
const theme = savedTheme || userTheme || (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
document.documentElement.setAttribute('data-theme', theme);
})();
		</script>
		<link rel="icon" href="/favicon.ico">
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
		<link rel="stylesheet" href="https://bootswatch.com/5/litera/bootstrap.min.css">
		<link rel="stylesheet" href="{{ asset('styles/premium.css') }}"> {% block stylesheets %}{% endblock %}
		<style>
			/* Modern animations */
			@keyframes fadeInUp {
				from {
					opacity: 0;
					transform: translateY(30px);
				}
				to {
					opacity: 1;
					transform: translateY(0);
				}
			}

			@keyframes slideInLeft {
				from {
					opacity: 0;
					transform: translateX(-30px);
				}
				to {
					opacity: 1;
					transform: translateX(0);
				}
			}

			@keyframes pulse {
				0,
				100% {
					transform: scale(1);
				}
				50% {
					transform: scale(1.05);
				}
			}

			/* Enhanced transitions */
			body {
				transition: background-color 0.3s ease;
			}

			.fade-in {
				animation: fadeInUp 0.6s ease-out;
			}

			.slide-in {
				animation: slideInLeft 0.5s ease-out;
			}

			.pulse {
				animation: pulse 2s infinite;
			}
		</style>
	</head>
	<body class="d-flex flex-column min-vh-100">
		{% if app.request.attributes.get('_route') not in ['app_home', 'app_login', 'app_register'] %}
			<nav class="navbar navbar-expand-lg premium-navbar shadow-sm py-3">
				<div class="container">
					<a class="navbar-brand fw-bold fs-3" href="{{ path('app_home') }}">
						<i class="fas fa-hammer me-2 text-primary"></i>Bricolage
					</a>
					<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
						<span class="navbar-toggler-icon"></span>
					</button>
					<div class="collapse navbar-collapse" id="navbarContent">
						<ul class="navbar-nav me-auto mb-2 mb-lg-0">
							<li class="nav-item">
								<a class="nav-link px-3" href="{{ path('app_social_feed') }}">
									<i class="fas fa-stream me-2"></i>
									{{ 'Feed'|trans }}</a>
							</li>
							{% if is_granted('ROLE_ADMIN') %}
								<li class="nav-item">
									<a class="nav-link px-3 fw-bold text-warning" href="{{ path('app_admin') }}">{{ 'Administration'|trans }}</a>
								</li>
							{% elseif is_granted('ROLE_RECRUTEUR') %}
								<li class="nav-item">
									<a class="nav-link px-3" href="{{ path('app_annonce') }}">{{ 'Mes Annonces'|trans }}</a>
								</li>
								<li class="nav-item">
									<a class="nav-link px-3" href="{{ path('app_recruteur_candidatures') }}">
										{{ 'Candidatures'|trans }}
										{% if app.user.recruteur is defined and app.user.recruteur is not null %}
											{% set cand_count = 0 %}
											{% for ad in app.user.recruteur.annonces %}
												{% set cand_count = cand_count + ad.candidatures|length %}
											{% endfor %}
											{% if cand_count > 0 %}
												<span class="badge bg-danger rounded-pill ms-1" style="font-size: 0.65rem;">{{ cand_count }}</span>
											{% endif %}
										{% endif %}
									</a>
								</li>
							{% elseif is_granted('ROLE_CHERCHEUR') %}
								<li class="nav-item">
									<a class="nav-link px-3" href="{{ path('app_annonce') }}">{{ 'Annonces'|trans }}</a>
								</li>
								<li class="nav-item">
									<a class="nav-link px-3" href="{{ path('app_candidature') }}">{{ 'Mes Candidatures'|trans }}</a>
								</li>
							{% endif %}
							<li class="nav-item">
								<a class="nav-link px-3" href="{{ path('app_marketplace') }}">{{ 'Marketplace'|trans }}</a>
							</li>
							<li class="nav-item">
								<a class="nav-link px-3" href="{{ path('app_chat') }}">
									<i class="far fa-comments me-1"></i>
									{{ 'Messagerie'|trans }}
								</a>
							</li>
						</ul>

						<div
							class="d-flex align-items-center gap-3">
							{# Theme Toggle #}
							<button id="theme-toggle" class="btn btn-link nav-link px-2" title="{{ 'Changer de thème'|trans }}">
								<i class="fas fa-moon fa-lg theme-icon-dark d-none"></i>
								<i class="fas fa-sun fa-lg theme-icon-light"></i>
							</button>
							<ul class="navbar-nav ms-auto align-items-center">
								<li class="nav-item dropdown me-2">
									<a class="nav-link dropdown-toggle" href="#" id="langDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
										<i class="fas fa-globe me-1"></i>
										{{ app.request.locale|upper }}
									</a>
									<ul class="dropdown-menu dropdown-menu-end shadow-sm border-0 mt-2" aria-labelledby="langDropdown">
										<li>
											<a class="dropdown-item" href="{{ path('app_change_locale', {'locale': 'fr'}) }}">Français (FR)</a>
										</li>
										<li>
											<a class="dropdown-item" href="{{ path('app_change_locale', {'locale': 'en'}) }}">English (EN)</a>
										</li>
										<li>
											<a class="dropdown-item" href="{{ path('app_change_locale', {'locale': 'ar'}) }}">العربية (AR)</a>
										</li>
									</ul>
								</li>
								{% if app.user %}
									<li class="nav-item dropdown">
										<a class="nav-link dropdown-toggle btn-profile-premium px-3" href="#" id="profileDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
											<i class="fas fa-user-circle me-1"></i>
											{{ app.user.userIdentifier }}
										</a>
										<ul class="dropdown-menu dropdown-menu-end shadow-sm border-0 mt-2" aria-labelledby="profileDropdown">
											<li>
												<a class="dropdown-item py-2" href="{{ path('app_profile') }}">
													<i class="fas fa-id-card me-2 opacity-50"></i>
													{{ 'Mon Profil'|trans }}
												</a>
											</li>
											<li><hr class="dropdown-divider"></li>
											<li>
												<a class="dropdown-item py-2 text-danger" href="{{ path('app_logout') }}">
													<i class="fas fa-sign-out-alt me-2 opacity-50"></i>
													{{ 'Déconnexion'|trans }}
												</a>
											</li>
										</ul>
									</li>
								{% else %}
									<li class="nav-item">
										<a class="nav-link px-3" href="{{ path('app_login') }}">{{ 'Connexion'|trans }}</a>
									</li>
									<li class="nav-item ms-lg-2">
										<a class="btn btn-primary px-4 rounded-pill shadow-sm" href="{{ path('app_register') }}">
											{{ 'Inscription'|trans }}
										</a>
									</li>
								{% endif %}
							</ul>
						</div>
					</div>
				</div>
			</nav>
		{% endif %}
		<div class="container mt-3">
			{% for label, messages in app.flashes %}
				{% for message in messages %}
					<div class="alert alert-modern alert-{{ label }} alert-dismissible fade show animate-slide-up shadow-sm rounded-3" role="alert">
						<div class="alert-content">
							{{ message|trans }}
							<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close">
								<i class="fas fa-times"></i>
							</button>
						</div>
					</div>
				{% endfor %}
			{% endfor %}
		</div>

		<main class="flex-grow-1"> {% block body %}{% endblock %}
			</main>

			<footer class="footer pt-5 pb-4 mt-auto">
				<div class="container">
					<div class="row">
						<div class="col-md-4 mb-4">
							<h5 class="fw-bold mb-4 text-primary">Bricolage</h5>
							<p class="text-muted small">{{ 'La plateforme de référence pour connecter les talents et les besoins en bricolage et services professionnels.'|trans }}</p>
						</div>
						<div class="col-md-4 mb-4">
							<h5 class="fw-bold mb-4">{{ 'Fondateurs'|trans }}</h5>
							<ul class="list-unstyled text-muted small">
								<li>Moubaraki Mohammed</li>
								<li>Benlaama Reda</li>
								<li>Traihy Soukaina</li>
							</ul>
						</div>
						<div class="col-md-4 mb-4">
							<h5 class="fw-bold mb-4">{{ 'Contactez-nous'|trans }}</h5>
							<ul class="list-unstyled text-muted small">
								<li>
									<i class="fas fa-envelope me-2"></i>moubaramed@gmail.com</li>
								<li>
									<i class="fas fa-phone me-2"></i>+212 66569562</li>
							</ul>
						</div>
					</div>
					<hr class="border-secondary mt-4 mb-4 opacity-10">
					<div class="text-center">
						<p class="text-muted small mb-0">&copy; 2026 Bricolage.
							{{ 'Tous droits réservés.'|trans }}</p>
					</div>
				</div>
			</footer>

			<!-- Mobile Bottom Navigation -->
			<nav class="bottom-nav">
				<a href="{{ path('app_home') }}" class="nav-item {{ app.request.attributes.get('_route') == 'app_home' ? 'active' : '' }}">
					<i class="fas fa-home"></i>
					<span>{{ 'Accueil'|trans }}</span>
				</a>
				<a href="{{ path('app_annonce') }}" class="nav-item {{ app.request.attributes.get('_route') in ['app_annonce', 'app_annonce_show'] ? 'active' : '' }}">
					<i class="fas fa-search"></i>
					<span>{{ 'Trouver'|trans }}</span>
				</a>

				{% if is_granted('ROLE_RECRUTEUR') %}
					<a href="{{ path('app_annonce_new') }}" class="nav-item nav-item-add">
						<div class="add-button">
							<i class="fas fa-plus"></i>
						</div>
					</a>
				{% else %}
					<a href="{{ path('app_marketplace') }}" class="nav-item {{ app.request.attributes.get('_route') == 'app_marketplace' ? 'active' : '' }}">
						<i class="fas fa-store"></i>
						<span>Shop</span>
					</a>
				{% endif %}

				<a href="{{ path('app_chat') }}" class="nav-item {{ app.request.attributes.get('_route') == 'app_chat' ? 'active' : '' }}">
					<i class="fas fa-comments"></i>
					<span>{{ 'Messages'|trans }}</span>
				</a>
				<a href="{{ path('app_profile') }}" class="nav-item {{ app.request.attributes.get('_route') == 'app_profile' ? 'active' : '' }}">
					{% if app.user %}
						<i class="fas fa-user-circle"></i>
					{% else %}
						<i class="fas fa-user"></i>
					{% endif %}
					<span>{{ 'Profil'|trans }}</span>
				</a>
			</nav>

			<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
			{% block javascripts %}{% endblock %}
			<script>
				// Enhanced theme toggle logic
document.addEventListener('DOMContentLoaded', function () {
const themeToggle = document.getElementById('theme-toggle');
const html = document.documentElement;
const darkIcon = themeToggle.querySelector('.theme-icon-dark');
const lightIcon = themeToggle.querySelector('.theme-icon-light');

function updateIcons(theme) {
if (theme === 'dark') {
darkIcon.classList.remove('d-none');
lightIcon.classList.add('d-none');
} else {
darkIcon.classList.add('d-none');
lightIcon.classList.remove('d-none');
}
}

// Sync icons with initial theme
updateIcons(html.getAttribute('data-theme'));

themeToggle.addEventListener('click', function () {
const currentTheme = html.getAttribute('data-theme');
const newTheme = currentTheme === 'dark' ? 'light' : 'dark';

html.setAttribute('data-theme', newTheme);
localStorage.setItem('theme', newTheme);
updateIcons(newTheme);

// Add smooth transition
document.body.style.transition = 'background-color 0.3s ease';
setTimeout(() => {
document.body.style.transition = '';
}, 300);
});

// Sync with server if logged in
{% if app.user %}
fetch("{{ path('app_theme_update') }}", {
method: 'POST',
headers: {
'Content-Type': 'application/json'
},
body: JSON.stringify(
{theme: newTheme}
)
}).catch(err => console.error('Failed to sync theme:', err));{% endif %}
});
			</script>
		</body>
	</html>
