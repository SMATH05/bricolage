{% extends 'base.html.twig' %}

{% block title %}
	{{ 'Mot de passe oublié'|trans }} | Bricolage
{% endblock %}

{% block stylesheets %}
	{{ parent() }}
	<style>
		.forgot-password-container {
			max-width: 400px;
			margin: 80px auto;
			padding: 40px;
			background: white;
			border-radius: 24px;
			box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
			border: 1px solid #e2e8f0;
		}

		.forgot-password-header {
			text-align: center;
			margin-bottom: 32px;
		}

		.forgot-password-title {
			font-size: 1.8rem;
			font-weight: 700;
			color: #1e293b;
			margin-bottom: 12px;
		}

		.forgot-password-subtitle {
			color: #64748b;
			font-size: 1rem;
			line-height: 1.5;
		}

		.form-control {
			border: 2px solid #e2e8f0;
			border-radius: 16px;
			padding: 16px 20px;
			font-size: 1rem;
			transition: all 0.3s ease;
		}

		.form-control:focus {
			border-color: #667eea;
			box-shadow: 0 0 0 4px rgba(102, 126, 234, 0.1);
			outline: none;
		}

		.btn-primary {
			background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
			border: none;
			border-radius: 16px;
			padding: 16px 24px;
			font-weight: 600;
			transition: all 0.3s ease;
			width: 100%;
		}

		.btn-primary:hover {
			transform: translateY(-2px);
			box-shadow: 0 8px 20px rgba(102, 126, 234, 0.3);
		}

		.back-to-login {
			text-align: center;
			margin-top: 24px;
		}

		.back-to-login a {
			color: #667eea;
			text-decoration: none;
			font-weight: 600;
			transition: color 0.3s ease;
		}

		.back-to-login a:hover {
			color: #764ba2;
		}

		@media (max-width: 768px) {
			.forgot-password-container {
				margin: 40px 16px;
				padding: 32px 24px;
			}
		}
	</style>
{% endblock %}

{% block body %}
	<div class="forgot-password-container">
		<div class="forgot-password-header">
			<h1 class="forgot-password-title">{{ 'Mot de passe oublié'|trans }}</h1>
			<p class="forgot-password-subtitle">
				{{ 'Entrez votre adresse email et nous vous enverrons un lien pour réinitialiser votre mot de passe.'|trans }}
			</p>
		</div>

		{% for label, messages in app.flashes %}
			{% for message in messages %}
				<div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
					{{ message|trans }}
					<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
				</div>
			{% endfor %}
		{% endfor %}

		<form method="post">
			{{ form_start(form) }}
			
			<div class="mb-4">
				{{ form_label(form.email, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
				{{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
				{{ form_errors(form.email) }}
			</div>

			<div class="mb-4">
				{{ form_widget(form.submit, {'attr': {'class': 'btn btn-primary'}}) }}
			</div>

			{{ form_end(form) }}
		</form>

		<div class="back-to-login">
			<a href="{{ path('app_login') }}">
				<i class="fas fa-arrow-left me-2"></i>
				{{ 'Retour à la connexion'|trans }}
			</a>
		</div>
	</div>
{% endblock %}
