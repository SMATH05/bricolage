{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --optimize-autoloader --no-scripts && php bin/console cache:clear --env=prod --no-warmup && php bin/console cache:warmup --env=prod"
  },
  "deploy": {
    "startCommand": "php bin/console cache:warmup --env=prod && vendor/bin/heroku-php-apache2 public/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
