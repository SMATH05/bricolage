{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "Dockerfile"
    },
    "deploy": {
        "startCommand": "sh -c \"php bin/console doctrine:migrations:migrate --no-interaction --allow-no-migration; apache2-foreground\""
    }
}